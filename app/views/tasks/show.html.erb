<h2><%= @task.title %></h2>
<p><%= @task.details %></p>

  <p>
    <input type="checkbox" <%= checked="checked" if @task.completed %>>
    <%= "This task is completed" if @task.completed %>
    <%= "This task is not completed yet" unless @task.completed %>
  </p>
<%= link_to "Back to list", tasks_path%>
